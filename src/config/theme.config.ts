// HeroUI theme configuration
// Themes are applied via CSS classes on the html element (light/dark)
// HeroUIProvider wraps the app but doesn't accept theme prop
// Theme is controlled by the @heroui/use-theme hook
// Custom themes can be configured in tailwind.config.js under heroui.themes
export const themeConfig = {
  // HeroUI themes are configured via Tailwind config and CSS classes
  // Theme state is managed by @heroui/use-theme hook
}

